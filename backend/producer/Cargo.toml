[package]
name = "producer"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio =  { version = "1", features = ["full"] }
redis = { version = "0.26.0", features = ["tokio-comp"] }
redisstream = { path = "../redisstream"}
store = { path = "../store"}